apiVersion: v1
kind: Service
metadata:
  name: {{ include "Hrubo-db.fullname" . }}
  labels:
    {{- include "Hrubo-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Hrubo-db.selectorLabels" . | nindent 4 }}
